import { CheckUserDirective } from './check-user.directive';

describe('CheckUserDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckUserDirective();
    expect(directive).toBeTruthy();
  });
});
