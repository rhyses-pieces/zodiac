import { PasscheckDirective } from './passcheck.directive';

describe('PasscheckDirective', () => {
  it('should create an instance', () => {
    const directive = new PasscheckDirective();
    expect(directive).toBeTruthy();
  });
});
